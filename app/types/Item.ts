export type Item = {
  id: number | null;
  description: string;
  quantity: number | string;
  price: number | string;
  total: number | string;
  discount: number | string;
  order_number: number | string;
};
